# Codex Manager v0.5.34

## 重点更新

- 启动后的 OpenCodex 自愈不再逐条等待供应商网络探测，避免某个上游慢响应拖住 Manager。
- 清理旧版本遗留的托管模型目录条目，只保留当前 OpenCodex 配置中的供应商和模型。
- 保留用户自定义供应商模型，不把自定义配置混入 OSIRAPI 迁移规则。

## 用户侧验收

从 v0.5.33 点击“立即更新”后，Manager 应在正常时间内启动；OpenCodex 服务保持 ready，Codex 模型目录只显示当前订阅模型。供应商可用性仍可通过模型管理中的“验证可用性”主动检查。
