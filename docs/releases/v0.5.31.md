# Codex Manager v0.5.31

## 重点更新

- 延续 v0.5.30 的 OSIRAPI 动态订阅供应商和 Gemini 模型支持。
- 修复正式 Release 草稿资产校验，确保更新清单、签名和发布绑定文件完整后才推进线上更新入口。

## 使用方式

旧版 Manager 打开更新检查，点击“立即更新”。更新并重启后，退出 OSIRAPI 连接再重新授权即可读取套餐当前支持的全部模型。

## 已知限制

套餐模型变化仍需要重新授权或重新同步；服务端和客户端必须使用同一批已发布版本。
