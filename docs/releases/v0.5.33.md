# Codex Manager v0.5.33

## 重点更新

- 修复 v0.5.32 发布前 CI Clippy 阻塞，确保所有平台构建可继续完成。
- 保留 OpenCodex 更新后的自动重载、供应商模型校验和旧配置迁移能力。
- 服务端候选分支继续优先返回可调用的 Gemini 推荐模型。

## 发布验收

更新后 Manager 会自动重载 OpenCodex，并校验已启用供应商的默认模型；如果校验失败，会保留重试状态并显示具体路由错误，不会把不可调用模型报告为可用。
