# Codex Manager v0.5.23

- 修复 OpenCodex 多模型启用后旧会话模型显示为空的问题。
- 在共享模型目录中保留旧版 Codex 裸模型 ID 兼容别名，历史会话可以继续显示、切换和发起对话。
- Manager 读取 OpenCodex 状态时自动补齐兼容目录；用户升级后无需再次关闭并启用多模型模式。
- 保留新的供应商前缀模型 ID，不影响 GPT、Claude、Grok 多模型路由。
