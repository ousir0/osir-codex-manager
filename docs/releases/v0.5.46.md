# Codex Manager v0.5.46

- 修复旧任务在供应商切换、重新登录或重启后继续对话返回 401 的问题：旧任务恢复使用的供应商元数据会随当前配置同步，避免第三方凭据误发到 OpenAI 官方接口。
- 关闭 OpenCodex 与 Manager 冲突的历史供应商重映射，避免修复在后续重启时失效。
- 修复普通供应商之间切换的历史任务衔接；保留未通过 Manager 管理的第三方供应商。
- 迁移保留消息和工具内容，先备份，并同步缓存字节位置；支持失败后继续恢复。
- 增强 Codex 桌面端和独立后端的运行检测；运行中更新会提示重启，磁盘空间不足会暂停迁移。

更新方式：在 Manager 中点击更新。若 Codex 仍在运行，待当前任务结束后，通过 Manager 的“重启 Codex”完成旧任务迁移；不需要重新安装或重新建立任务。

验证：真实 Codex 加载故障任务隔离副本，完成六次冷启动续聊和模式往返测试，请求接口及凭据均与当次配置一致。实际供应商的账户权限、凭据有效性仍由其服务决定。
