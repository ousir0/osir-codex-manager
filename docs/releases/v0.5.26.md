# Codex Manager v0.5.26

- Fixes OpenCodex activation to use a named custom Codex provider instead of masquerading as the built-in `openai` provider.
- Shows configured GPT, Claude, and Grok routed models in Codex's selector.
- Hides bare GPT compatibility aliases from new selections while preserving legacy session resolution.
- Stabilizes catalog ordering as GPT → Claude → Grok and persists corrections to existing catalogs.
