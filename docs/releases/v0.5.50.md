# v0.5.50

- osirAPI GPT 授权路由自动绑定原生图片工具，聊天与生图复用同一个 Key，无需单独选择生图技能。
- 默认图片模型为 GPT Image 2.5 (flare)；需要服务器支持该偏好，且订阅分组允许该模型，按该模型计费。
- 已启用的旧管理器配置会自动补齐原生生图配置；授权或升级后请完全退出并重新打开 Codex，再新建对话。
- 保留用户明确设置的自定义图片供应商；无效的自定义路由会报错，避免静默切换计费渠道。
- 本次不改变第三方 CC-Switch 的配置保留行为。

验证：Rust 全量测试 248 通过、1 忽略；已隔离验证 Codex 原生图片工具携带图片模型偏好和同密钥请求。
Windows 安装器仍未完成 Authenticode 签名，可能出现 SmartScreen 提示；请使用发布文件中的 SHA256SUMS 核验。
