# v0.5.47

- 修复会话修复与 Codex 启动并发导致的重启报错。
- 自动修复和索引迁移按最近活动时间排序，只处理最近 100 个会话。
- 新增 Codex 会话备份与可恢复归档脚本：scripts/retain-recent-codex-sessions.py。
- 版本升级后用户通过 Manager 的“立即更新”生效。

验证：npx eslint src --max-warnings=0、cargo check --manifest-path src-tauri/Cargo.toml --workspace --locked、脚本语法检查通过。
