# Codex Manager v0.5.39

## 重点更新

- 修复 Windows 用户 OSIRAPI 授权后 OpenCodex 无法启动的问题。
- 修复跨平台构建 OpenCodex 组件时 Bun 运行时可能被打包成构建机格式的问题。
- 对已有组件增加 Windows Bun 运行时自修复；用户无需手动删除授权配置。
- 增加组件发布前的 Windows 可执行文件格式检查。

## 用户影响

升级到 v0.5.39 后，重新点击 OSIRAPI 授权即可。已有账号配置和会话不会被删除。
