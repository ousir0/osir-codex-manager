# Codex Manager v0.5.22

- Fixes OpenCodex 2.22 activation to use its supported Design B Codex injection: `model_provider = "openai"` with root `openai_base_url` and the shared model catalog.
- Prevents the manager from leaving Codex pointed at a local proxy that OpenCodex did not inject.
- Preserves native Codex history identifiers while routing active requests through OpenCodex.
