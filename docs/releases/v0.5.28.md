# Codex Manager v0.5.28

- 修复 OSIRAPI 授权完成后，临时 `502`、限流、超时或连接重置被误判为授权失败的问题。
- 临时上游故障会自动重试，并显示为“临时异常”；用户无需重新授权，可直接重新检测。
- 修复完整 `供应商/模型` 路由的健康状态读取，让 Manager 的连接状态与模型实际可用性保持一致。
- 保留确定性鉴权错误的明确失败提示，避免把无效凭据误报为网络波动。

更新后如已打开 Codex，请完全退出并重新打开一次，让原生模型选择器读取最新状态。
