# Codex Manager v0.5.38

## 重点更新

- 修复订阅 OAuth 兑换后 Gemini Provider 和模型未进入 OpenCodex 的问题。
- 接收并校验服务端返回的完整动态 Provider/模型集合，防止摘要与加密配置不一致时写入半套配置。
- 修复 OpenCodex 命令超时期间 stdout/stderr 管道可能阻塞的问题。
- 移除 OpenCodex 版本永久缓存，更新组件后状态页可读取实际版本。
- 保留现有配置、订阅状态、API Key、会话和主题数据；升级后如需重新授权，由用户手动完成。

## 验证说明

- TypeScript 类型检查、相关前端测试、OpenCodex Rust 测试和服务端 Codex 相关 Go 测试通过。
- 全量 ESLint 仍受仓库既有压缩课件文件 `ppt/codex-course-share/assets/motion.min.js` 的历史错误影响。
